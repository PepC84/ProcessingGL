// Processing_defaults.cpp — no longer needed.
// Windows event wiring is now done via registerCallbacks() in setup().
// This file is kept as a placeholder so build scripts that include it
// compile without error. It defines nothing.
#ifdef _WIN32
// intentionally empty
#endif
